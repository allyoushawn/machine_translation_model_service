from setuptools import setup, find_packages

setup(
    name="mt_model_service",
    version="1.0",
    description="Machine translation model service",
    include_package_data=True,
    packages=find_packages()
)